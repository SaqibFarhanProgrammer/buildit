# BuildIt — Next Day Tasks (Post Authentication Phase)

## Current Status

Authentication system is completed and stable. OAuth flow is working, user session creation is functional, and basic protected routing is in place.

The next phase focuses on UI refinement, API-level data consistency, and improving rendering logic for core features (especially rating-related data handling).

---

## Day Task Breakdown

### 1. Rating System Fix (API + UI Sync)

* Ensure rating data is correctly coming from API responses
* Validate schema consistency in backend (MongoDB model check)
* Fix missing or undefined rating fields in API output
* Normalize rating values (avoid null/undefined cases)

---

### 2. Frontend Rendering Improvements

* Improve UI rendering for rating components
* Handle edge cases (no rating, loading state, empty data)
* Ensure smooth conditional rendering (no UI flicker)

---

### 3. API Response Cleanup

* Standardize API response structure
* Ensure all endpoints return consistent format:

  * success
  * data
  * error (if any)
* Remove inconsistent or partial payloads

---

### 4. Data Validation Layer

* Add server-side validation for incoming data
* Prevent invalid rating submissions
* Sanitize user inputs before DB storage

---

### 5. Debugging & Logging

* Add temporary logs for rating-related API routes
* Track missing or broken responses
* Identify mismatch between DB and frontend expectations

---

## Engineering Focus

* Stability over features
* Data consistency between API and UI
* Clean separation between backend logic and frontend rendering

---

today taski inshallah  <--
## Expected Outcome

* Rating system fully stable
* No missing or broken UI states 
* Clean API structure ready for scaling
* Frontend rendering fully aligned with backend data


working on code and projects adn sessions 

# Step 1 — Project Creation System
- Create project schema
- Create project API
- Create project creation form
- Save project in database
- Show projects on dashboard

---

# Step 2 — Single Project Page
- Create dynamic route
- Fetch project by ID
- Show project details
- Handle loading and error states

Example:
`/project/[projectId]`

---

# Step 3 — Connect Project To Code Editor
- Open editor from project
- Pass project ID into editor
- Load project data inside editor
- Create editor layout

---

# Step 4 — Save & Fetch Code
- Create code schema
- Save code into database
- Fetch code when editor opens
- Auto-load latest saved code

---

# Step 5 — Editor Activity Tracking
- Detect typing activity
- Detect idle state
- Track last active timestamp
- Create activity state system

---

# Step 6 — Session Creation System
- Create session schema
- Start coding session
- Store session in DB
- Add session status

Example:
active
paused
completed

---

# Step 7 — Timer System
- Start timer on session start
- Run timer locally on frontend
- Handle pause/resume
- Calculate total duration

---

# Step 8 — Session Sync Logic
- Save session on code save
- Auto-sync every few minutes
- Handle refresh recovery
- Handle tab close

---

# Step 9 — Dashboard Analytics
- Total coding hours
- Daily activity
- Weekly stats
- Most used language
- Session history

---

# Step 10 — Gamification
- XP system
- Streak system
- Levels
- Leaderboard
- Achievements